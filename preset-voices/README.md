# Preset Voice Library

Put authorized reference voice files in `preset-voices/audio` and describe them in `manifest.json`.

The backend validates that every manifest file path stays inside this directory. Preset voices are read-only from the UI and can be selected as role reference samples.
